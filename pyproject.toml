[project]
name = "ulix"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "init-file-checker>=0.0.2",
    "mypy>=1.16.1",
    "neo4j>=5.28.1",
    "pydantic>=2.11.7",
    "ruff>=0.12.0",
]
